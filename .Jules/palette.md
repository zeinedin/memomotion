# Palette's Journal

## 2024-05-22 - Accessibility in Custom Toggles
**Learning:** Custom button groups using divs and CSS classes need manual ARIA management (`role="group"`, `aria-pressed`) to be accessible to screen readers.
**Action:** Always check `aria-pressed` state management when using non-native radio/checkbox inputs.
