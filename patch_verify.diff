--- verification/verify_frontend.py
+++ verification/verify_frontend.py
@@ -58,7 +58,7 @@

         # 3. Assert error state
         expect(error_container).to_be_visible()
-        expect(error_container).to_have_text("Crew name is required!")
+        expect(error_container).to_have_text("Enter a team name (min 2 chars)")
         assert input_element.get_attribute("aria-invalid") == "true"
         # Since style.borderColor is set to var(--neon-red), we evaluate it directly
         border_color = input_element.evaluate("el => el.style.borderColor")
